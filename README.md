# Climate Memory + Evidence — final integrated revision

This build uses the simplified observation → tool setup → guided interpretation → comparison flow.

Resource pages now use the actual interface labels learners encounter in NOAA Climate at a Glance, Drought.gov, and the Climate Toolbox Historical Climate Tracker. Pattern interpretation is handled through pathway-specific response choices rather than repeated explanatory text. Optional notes are transferred with the guided answers.

The paired Garden Journal package in the final bundle is required for the structured handoff display.
